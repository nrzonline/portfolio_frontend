import { Injectable } from '@angular/core'


/**
 * The data-service is used for remembering certain data throughout the
 * application, which are not lost between routes.
 */

export class DataService {
    public constructor(){
    }
}
